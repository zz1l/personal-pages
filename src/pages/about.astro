---
import BaseLayout from '../layouts/BaseLayout.astro';
import { SITE } from '../config';

const base = import.meta.env.BASE_URL;
const withBase = (p: string) => (base === '/' ? p : base.replace(/\/$/, '') + p);
---

<BaseLayout title="关于 · 个人知识库" description="关于这个知识库以及它的运作方式。">
  <div class="wrap py-10 max-w-3xl">
    <header class="mb-10">
      <h1 class="text-2xl font-bold tracking-tight m-0">关于</h1>
    </header>

    <div class="prose">
      <p>
        这是我的个人知识库——一座<em>数字花园</em>，而不是博客。
        这里的一切都从一个 Markdown 文件开始，然后像植物一样自然生长：
        笔记会被持续修订、互相关联、重新整理。{SITE.subtitle}。
      </p>
      <p>{SITE.tagline}</p>

      <h2>笔记如何成熟</h2>
      <p>每篇笔记都带有一个描述其成熟度的状态：</p>
      <ul>
        <li><strong>种子</strong>——一篇非常早期的笔记：粗略、不完整、探索性的。</li>
        <li><strong>萌芽</strong>——正在成形，但仍是碎片。</li>
        <li><strong>生长中</strong>——正在积极发展与修订。</li>
        <li><strong>常青</strong>——成熟且相对稳定。</li>
      </ul>
      <p>
        与发布日期不同，这种成熟度模型描述的是你今天能在多大程度上信任一篇笔记。
      </p>

      <h2>本站如何运作</h2>
      <p>
        笔记是带 YAML frontmatter 的纯 Markdown 文件。构建时，Astro 会静态生成每一个页面：
        笔记浏览器、<a href={withBase('/graph/')}>知识图谱</a>（由 <code>[[wiki 链接]]</code> 构建）、
        <a href={withBase('/stats/')}>统计</a>、<a href={withBase('/timeline/')}>时间线</a>、
        搜索索引和 RSS 订阅。没有数据库、没有服务器——一切都以静态文件的形式部署到 GitHub Pages。
      </p>
      <p>
        数学公式由 KaTeX 排版，代码由 Shiki 高亮，图表由 Mermaid 渲染，
        字体使用 Inter 与 JetBrains Mono。
      </p>

      <h2>联系</h2>
      <p>
        可以在 <a href={SITE.social.github} target="_blank" rel="noopener">GitHub</a> 上找到我，
        或给我发<a href={SITE.social.email}>邮件</a>。订阅 <a href={withBase(SITE.social.rss)}>RSS</a> 获取更新。
      </p>
    </div>
  </div>
</BaseLayout>
